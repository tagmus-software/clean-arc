describe("Event decorators", () => {
    describe("MessagePattern", () => {
        test.todo(
            "should push event consumer with the necessary properties to the variable registered consumers "
        );
    });
});
